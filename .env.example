# Video Downloader Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# Course Platform Credentials (OPTIONAL)
# ============================================
# Only needed if downloading from authenticated course platforms
# Leave empty if you're only downloading from public sites (YouTube, TikTok, etc.)
#
# Supported platforms: Hub.la, Código Viral, Hotmart, Eduzz, Kiwify, Monetizze
# These credentials are stored locally and never sent to external servers
# except the course platform's own authentication servers

COURSE_LOGIN=your_email@example.com
COURSE_PASSWORD=your_password

# ============================================
# Download Settings (OPTIONAL)
# ============================================

# Directory where downloaded videos will be saved
# Default: ~/Downloads/VideoDownloader
DOWNLOAD_DIR=~/Downloads/VideoDownloader

# Maximum number of simultaneous downloads
# Default: 3
# Reduce this if downloads are slow or your connection is limited
MAX_CONCURRENT_DOWNLOADS=3

# ============================================
# FFmpeg Configuration (OPTIONAL)
# ============================================
# Path to FFmpeg binary
# Leave commented out for automatic detection
# The system will automatically search in:
#   - /opt/homebrew/bin (macOS Homebrew ARM)
#   - /usr/local/bin (macOS Homebrew Intel, Linux)
#   - server/bin/ (bundled binaries)
#   - System PATH

# FFMPEG_LOCATION=/opt/homebrew/bin/ffmpeg

# ============================================
# Flask Server Settings (OPTIONAL)
# ============================================

# Enable debug mode (true/false)
# Default: false
# Only enable for development/troubleshooting
# FLASK_DEBUG=false
